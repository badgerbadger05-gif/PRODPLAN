# Журнал заказов на производство: целевой контур

## Цель

Журнал заказов становится рабочим контуром между MRP, складом, цехом и 1С:

1. MRP рассчитывает потребность, даты запуска/окончания, мощности и дефициты.
2. Продплан создает внутренние заказы на производство из MRP-строк.
3. Эти заказы учитываются в следующих MRP-расчетах как активное ожидаемое производство.
4. В журнале пользователь запускает выбранные строки в 1С.
5. Продплан создает в 1С заказы на производство и документы перемещения компонентов.
6. Проведение документов остается на стороне 1С.
7. Продплан обновляет состояние по факту проведенных документов 1С.

## Принятые правила

- Прямой деплой на прод мимо GitHub запрещен без отдельного явного разрешения.
- Документы в 1С из Продплана создаются непроведенными (`Posted=False`).
- Проведение выполняется пользователями/регламентами на стороне 1С.
- Для повторного нажатия нужна идемпотентность: один `PlannedOrder` не должен создавать несколько внутренних заказов.
- Внутренние заказы Продплана хранятся в тех же таблицах, что и заказы 1С:
  - `production_orders.source = 'mrp'`;
  - `production_orders.source_run_id = planning_run.run_id`;
  - `production_products.source_planned_order_id = planned_order.order_id`.

## Состояния обеспечения

Колонка журнала называется **Обеспечение**.

Статусы:

- `Дефицит` (`shortage`) — нет обеспечения по одному или нескольким компонентам.
- `Частично` (`partial`) — часть компонентов есть, но не на весь заказ.
- `Обеспечен` (`ready`) — компонентов хватает на разрешенных складах.
- `К перемещению` (`to_move`) — документы в 1С созданы, ожидается проведение/физическое перемещение.
- `Собран` (`assembled`) — Продплан нашел проведенные перемещения в 1С.
- `Произведен частично` (`produced_partial`) — выпуск меньше заказа.
- `Произведен` (`produced`) — выпуск закрыл заказ.

Отдельный статус `Перемещается` не используется: он дублирует `К перемещению`.

## Настройки

В журнале есть окно настроек:

- привязка `участок -> склад получатель`;
- список игнорируемых складов.

Игнорируемые склады нужны, чтобы не задавать лишние вопросы по остаткам, например если компонент лежит в изоляторе брака.

## Реализованный первый срез

- Добавлены поля источника на производственные заказы и строки.
- Добавлены настройки складов по участкам и игнорируемых складов.
- Добавлен endpoint создания внутренних заказов из MRP.
- Добавлен endpoint подготовки запуска выбранных строк в 1С.
- Журнал показывает колонку `Обеспечение`.
- Окно компонентов показывает доступность: достаточно, частично, дефицит.
- Для недостающих компонентов выводятся ближайшие ожидаемые даты из заказов поставщику или производства.

## Следующие этапы

1. Подтвердить обязательные реквизиты 1С для:
   - `Document_ЗаказНаПроизводство`;
   - `Document_ПеремещениеЗапасов`;
   - `Document_СборкаЗапасов`;
   - `Document_СдельныйНаряд`.
2. Добавить реальные write-сервисы 1С по паттерну `one_c_purchase_order_export.py`.
3. Добавить локальные таблицы связей с созданными документами 1С, если одного payload/ref на строке будет недостаточно.
4. Добавить sync проведенных перемещений и автоматический переход `К перемещению -> Собран`.
5. Реализовать кнопку `Произвести`: запрос количества и исполнителя, создание производства и сдельного наряда.
6. При частичном выпуске создавать обратное перемещение лишних компонентов на исходные склады.
7. Добавить разрез остатков по складам в локальную БД, чтобы автоматически выбирать склады источники и задавать вопрос только при неоднозначности.
