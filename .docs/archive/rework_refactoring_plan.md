# План рефакторинга: новый способ пополнения `переработка`

## Контекст

- [x] Подтвердить, что `переработка` реализуется как отдельный поток результатов MRP, а не как разновидность закупки
- [x] Сохранить текущее корректное поведение потока производства без изменения бизнес-результатов
- [x] Устранить ошибку с дробными количествами в закупке для дискретных единиц измерения
- [x] Обеспечить поддержку спецификаций и дефицита комплектующих для потока `переработка`
- [x] Довести отображение групп товаров до стабильного состояния: сохранение, повторное открытие страницы, группировка в UI и выгрузке

## Ограничения и правила выполнения

- [ ] Не начинать изменения кода без явного подтверждения пользователя на реализацию
- [x] Перед каждым этапом реализации сверяться с документацией в `.docs/README.md`, `.docs/architecture.md`, `.docs/api.md`, `.docs/db_schema.md`
- [ ] Не ломать текущие API-контракты без отдельного решения; новые контракты добавлять рядом с существующими
- [ ] Все изменения БД сопровождать отдельной миграцией Alembic
- [ ] Все изменения проектировать так, чтобы их мог выполнять ИИ пошагово, малыми, проверяемыми коммитоподобными итерациями
- [ ] После выполнения каждого пункта или итерации сразу отмечать его чекбоксом `[x]` в этом плане
- [ ] После завершения реализации обновить `.docs/progress.md`
- [x] После завершения реализации обновить `.docs/progress.md`

## Целевая архитектурная идея

- [x] Разделить расчёт MRP на 3 независимых потока: `production`, `purchase`, `rework`
- [x] Выделить общий классификатор способа пополнения, чтобы логика определения потока не была размазана по сервисам
- [x] Вынести общий слой нормализации количества, округления и чтения свойств единицы измерения
- [x] Оставить production как эталонный поток, от которого можно переиспользовать логику дефицита комплектующих
- [x] Ввести отдельную сущность результатов для `rework`, отдельные API-методы, отдельное отображение в UI и отдельную Excel-выгрузку

## Основные точки входа в кодовую базу

### Backend

- [x] Проанализировать классификацию потока в `backend/app/services/planning_service.py`
- [x] Проанализировать расчёт количества и ограничения по комплектующим в `backend/app/services/order_quantity_calculator.py`
- [x] Проанализировать модели результатов MRP в `backend/app/models.py`
- [x] Проанализировать API результатов в `backend/app/routers/plan.py`
- [x] Проанализировать API выбора групп в `backend/app/routers/odata.py`
- [x] Проанализировать текущие экспортные сервисы Excel

### Frontend

- [x] Проанализировать страницу синхронизации групп в `frontend/src/pages/SyncPage.vue`
- [x] Проанализировать страницу результатов MRP в `frontend/src/pages/MRPResultPage.vue`
- [x] Проанализировать composable закупок в `frontend/src/composables/usePurchases.ts`
- [x] Проанализировать существующие типы MRP в `frontend/src/types/mrp.ts`
- [x] Проанализировать сервис API на фронтенде в `frontend/src/services/api.ts`

## Фаза 1. Подготовка к рефакторингу

### 1.1. Зафиксировать классификацию способов пополнения

- [x] Найти все места, где используется `items.replenishment_method`
- [x] Составить таблицу соответствий исходных значений 1С и внутренних потоков `production`, `purchase`, `rework`
- [x] Отдельно выделить возможные синонимы для закупки и переработки
- [x] Зафиксировать поведение по умолчанию для неизвестных значений `replenishment_method`
- [x] Решить, нужен ли отдельный helper-модуль для классификации потока
- [x] Подготовить список файлов, которые должны перейти на единый классификатор

### 1.2. Зафиксировать инварианты production-потока

- [x] Выписать участки кода, которые нельзя менять по поведению без регрессии
- [x] Выделить набор тестов, которые подтверждают корректность production
- [x] Зафиксировать expected behavior для active 1C orders, component shortage и capacity scheduling
- [x] Подготовить контрольный чеклист: какие поля и расчёты production должны остаться неизменными

### 1.3. Зафиксировать текущее поведение групп товаров

- [x] Найти, где хранится список выгруженных групп товаров
- [x] Найти, где хранится выбранный набор активных групп
- [x] Убедиться, что backend уже сохраняет выбор и проблема находится в UI-гидрации или в сценарии загрузки страницы
- [x] Найти, какие модели или API уже содержат ссылку на группу товара
- [x] Зафиксировать, чего не хватает для группировки закупки и переработки по товарным группам

## Фаза 2. Проектирование backend-изменений

### 2.1. Рефакторинг классификации потока пополнения

- [x] Создать отдельный проектный шаг для выноса классификации потока из `build_planned_orders_and_purchases`
- [x] Спроектировать helper-функцию или service-level utility с единым интерфейсом
- [x] Определить возвращаемое значение классификатора: строка, enum-подобное значение или dataclass
- [x] Убедиться, что классификатор может использоваться и в расчёте, и в API, и в экспорте
- [x] Добавить в план тесты на классификацию всех допустимых значений

### 2.2. Проектирование отдельной сущности `rework`

- [x] Проверить, хватает ли существующей модели `planned_purchase` для `rework`
- [x] Если не хватает, спроектировать новую таблицу, например `planned_rework`
- [x] Определить обязательные поля новой сущности
- [x] Определить, нужны ли поля `requested_qty`, `planned_qty`, `qty`, `need_date`, `order_date`, `lead_time_days`
- [x] Определить, нужно ли хранить ссылку на спецификацию для `rework`
- [x] Определить, нужно ли хранить признаки частичной блокировки по комплектующим
- [x] Определить, нужно ли хранить товарную группу денормализованно или подтягивать join-ом
- [x] Подготовить список изменений для `backend/app/models.py`
- [x] Подготовить список изменений для `backend/app/schemas.py`
- [x] Подготовить список изменений для миграции Alembic

Примечание по итогам анализа:

- текущая `planned_purchase` подходит только для закупки и не содержит полей для спецификации, диагностик дефицита комплектующих и признаков частичной/полной блокировки;
- существующие purchase-endpoint'ы и экспорт закупки уже завязаны на плоский закупочный контракт, поэтому безопаснее вводить отдельную модель и отдельные API-контракты для `rework`, а не расширять закупку условными полями;
- выбор групп товаров сейчас хранится отдельно файлово, поэтому товарную группу для будущих grouped-endpoint'ов лучше получать через связь номенклатуры, а не денормализовать выбор пользователя в строке результата.

### 2.3. Разделение расчёта на независимые обработчики

- [x] Разбить текущий монолитный проход по требованиям на отдельные ветки `production`, `purchase`, `rework`
- [x] Спроектировать отдельные функции построения заказа для каждого потока
- [x] Выделить общую функцию нормализации количества
- [x] Выделить общую функцию определения дискретности единицы измерения
- [x] Выделить общую функцию вычисления даты заказа с учётом lead time
- [x] Определить, какие входные аргументы должны передаваться в каждый обработчик
- [x] Убедиться, что production-ветка после выделения функций не меняет алгоритм расчёта

### 2.4. Исправление закупки

- [x] Проанализировать, почему закупка сейчас использует `requested_qty_raw` без нормализации
- [x] Спроектировать единую политику округления для дискретных единиц измерения
- [x] Определить, как округлять закупку при дробном net-demand: вниз, вверх или по правилам единицы
- [x] Зафиксировать, какие поля должны хранить диагностическое значение, а какие — итоговое бизнес-количество
- [x] Вынести корректировку количества так, чтобы её можно было переиспользовать в `rework`
- [x] Подготовить отдельный набор тестов именно на закупку

### 2.5. Проектирование логики `rework`

- [x] Зафиксировать, что `rework` использует спецификации, как и production
- [x] Проверить, можно ли использовать текущий `OrderQuantityCalculator` без изменения production-логики
- [x] Определить, нужен ли отдельный режим работы калькулятора для `rework`
- [x] Зафиксировать, какие правила дефицита комплектующих для production должны полностью переноситься в `rework`
- [x] Зафиксировать, какие правила production не должны переноситься в `rework`, например этапы и мощности, если это не требуется бизнесом
- [x] Определить, как рассчитывать `planned_qty` при полном дефиците комплектующих
- [x] Определить, как рассчитывать `planned_qty` при частичном дефиците комплектующих
- [x] Определить формат warning-кодов для `rework`
- [x] Проверить, нужно ли резервирование компонентов из активных 1С-заказов распространять на `rework`

### 2.6. Проектирование API результатов для `rework`

- [x] Добавить в план новый endpoint списка заказов на переработку
- [x] Добавить в план новый endpoint группированного представления заказов на переработку
- [x] Добавить в план новый endpoint Excel-экспорта заказов на переработку
- [x] Определить response schema для строк `rework`
- [x] Определить response schema для группировки по товарным группам
- [x] Проверить, нужно ли расширять summary run-а новым счётчиком `rework_requests`
- [x] Проверить, нужно ли менять существующий response для закупки

## Фаза 3. Проектирование группировки по товарным группам

### 3.1. Backend-данные по группам товаров

- [x] Проверить, в какой сущности хранится группа товара после синхронизации номенклатуры
- [x] Если группа не хранится явно, определить способ её добавления в модели и синхронизацию
- [x] Определить, как связать строку закупки с группой товара через item
- [x] Определить, как связать строку переработки с группой товара через item
- [x] Подготовить список join-ов и индексов, которые потребуются для grouped endpoints

### 3.2. Группировка закупки

- [x] Найти существующую реализацию группировки производства как референс
- [x] Спроектировать аналогичный grouped endpoint для закупки, но по товарной группе вместо участка
- [x] Определить состав полей группы: `group_id`, `group_name`, `orders`, `sum_qty`, дополнительные метрики
- [x] Определить политику сортировки групп
- [x] Определить политику попадания строк без группы в отдельную секцию

### 3.3. Группировка переработки

- [x] Спроектировать grouped endpoint для `rework` по той же схеме, что и для закупки
- [x] Определить, нужны ли отдельные флаги по дефициту комплектующих на уровне строки и группы
- [x] Подготовить единый контракт группировки для UI, чтобы фронтенд не дублировал логику

## Фаза 4. Проектирование Excel-выгрузок

### 4.1. Закупка

- [x] Найти существующий экспорт закупок или отсутствие такого сервиса
- [x] Если экспорта закупки нет, спроектировать новый Excel service
- [x] Разбить закупки по товарным группам внутри Excel
- [x] Зафиксировать колонки для закупки
- [x] Зафиксировать правила форматирования количеств и единиц измерения
- [x] Зафиксировать правила отображения строк без группы

### 4.2. Переработка

- [x] Спроектировать отдельный Excel service для `rework`
- [x] Зафиксировать колонки для переработки
- [x] Решить, отображать ли сведения о спецификации и дефиците комплектующих прямо в Excel
- [x] Разбить строки по товарным группам аналогично закупке
- [x] Зафиксировать единые правила форматирования и сортировки

### 4.3. Общие требования к экспорту

- [x] Зафиксировать единый подход к именам файлов
- [x] Зафиксировать единый подход к количествам с учётом дискретности
- [x] Зафиксировать единый подход к пустым секциям и отсутствующим данным
- [x] Зафиксировать, что production export не должен измениться без отдельной задачи

## Фаза 5. Проектирование frontend-изменений

### 5.1. Страница синхронизации групп

- [x] Спроектировать сценарий инициализации страницы так, чтобы при открытии сразу подгружались список групп и сохранённый выбор
- [x] Проверить, нужно ли вызывать загрузку групп на `onMounted`
- [x] Проверить, не перетирается ли `selectedIds` при повторной загрузке списка
- [x] Убедиться, что после сохранения состояние выбора остаётся консистентным в памяти компонента
- [x] Добавить в план отдельную UI-проверку: открыть страницу повторно и увидеть ранее сохранённые группы
- [ ] Продумать отображение для случая, когда список групп пуст, а выбор сохранён

### 5.2. Страница результатов MRP

- [x] Определить, где на странице разместить отдельный блок `rework`
- [x] Определить, какие существующие блоки `purchase` нужно доработать для группировки по товарным группам
- [x] Определить, нужен ли новый composable `useRework.ts`
- [x] Если нужен отдельный composable, зафиксировать его API и состояние
- [x] Добавить отдельные frontend-типы для строк и групп `rework`
- [x] Добавить отдельные API-вызовы для `rework`
- [x] Спроектировать поведение пагинации и сортировки для новых таблиц
- [x] Спроектировать поведение экспорта из UI для новых таблиц

### 5.3. Повторное использование UI-логики

- [x] Найти, какие компоненты production/purchase можно переиспользовать для `rework`
- [x] Определить, стоит ли создать универсальный компонент grouped table по товарным группам
- [x] Определить, какие форматтеры количества и единиц должны стать общими
- [x] Зафиксировать минимальный объём изменений в существующих компонентах, чтобы снизить риск регрессии

## Фаза 6. Детализация тестирования

### 6.1. Unit-тесты backend

- [x] Добавить тесты на классификатор способа пополнения
- [x] Добавить тесты на отсутствие дробных `planned_qty` у закупки для дискретных единиц
- [x] Добавить тесты на сохранение дробных значений только там, где единица действительно недискретна
- [x] Добавить тесты на создание `rework`-заказа при наличии достаточных комплектующих
- [x] Добавить тесты на частичный `rework`-заказ при ограничении по комплектующим
- [x] Добавить тесты на блокировку `rework`-заказа при полном дефиците комплектующих
- [x] Добавить тесты на неизменность production-ветки по контрольным сценариям

### 6.2. API-тесты backend

- [x] Добавить тесты на endpoint списка `rework`
- [x] Добавить тесты на grouped endpoint `rework`
- [x] Добавить тесты на grouped endpoint закупки по товарным группам
- [x] Добавить тесты на Excel-export endpoints
- [x] Добавить тесты на корректные поля группировки и суммы по группам

### 6.3. UI-проверки

- [x] Проверить сохранение и восстановление выбора групп на странице синхронизации
- [x] Проверить отображение закупки по товарным группам
- [x] Проверить отображение переработки по товарным группам
- [x] Проверить экспорт кнопками из UI
- [ ] Проверить, что существующий production-блок отображается как раньше

### 6.4. Регрессия

- [x] Прогнать тесты, покрывающие текущий расчёт production
- [x] Сравнить summary результата до и после рефакторинга на контрольном наборе данных
- [x] Сравнить production orders до и после рефакторинга на контрольном run-е
- [x] Убедиться, что warnings production сохранили прежний формат

## Фаза 7. Пошаговая стратегия реализации для ИИ

### 7.1. Последовательность безопасных итераций

- [ ] Итерация 1: добавить markdown-спецификацию и зафиксировать архитектурные решения
- [x] Итерация 2: вынести классификатор потоков без изменения поведения
- [x] Итерация 3: вынести общую нормализацию количеств и покрыть тестами
- [x] Итерация 4: исправить закупку и доказать тестами исчезновение дробных количеств
- [x] Итерация 5: добавить новую модель `rework` и миграцию
- [x] Итерация 6: добавить backend-расчёт `rework` без UI
- [x] Итерация 7: добавить API списка и группировки `rework`
- [x] Итерация 8: добавить группировку закупки по товарным группам
- [x] Итерация 9: добавить Excel-выгрузки закупки и переработки
- [x] Итерация 10: исправить страницу синхронизации групп
- [x] Итерация 11: добавить UI для `rework` и доработать UI закупки
- [x] Итерация 12: провести регрессию production и обновить документацию

### 7.2. Правила для каждой итерации

- [ ] Перед началом каждой итерации ИИ должен явно перечислить изменяемые файлы
- [ ] Каждая итерация должна иметь локальную цель и проверяемый результат
- [ ] Нельзя смешивать в одной итерации backend-архитектуру, UI и экспорт без необходимости
- [ ] После каждой итерации ИИ должен описать, что именно проверено
- [ ] Если в ходе итерации вскрывается неизвестная зависимость, ИИ должен сначала обновить план, а потом продолжить реализацию

## Фаза 8. Список предполагаемых изменений по файлам

### Backend: обязательно проверить

- [x] `backend/app/services/planning_service.py`
- [x] `backend/app/services/order_quantity_calculator.py`
- [x] `backend/app/models.py`
- [x] `backend/app/schemas.py`
- [x] `backend/app/routers/plan.py`
- [x] `backend/app/routers/odata.py`
- [x] `backend/app/services/nomenclature_sync.py`
- [x] `backend/app/services/production_order_export.py`
- [x] новый сервис экспорта закупки
- [x] новый сервис экспорта переработки
- [x] новая Alembic-миграция в `backend/alembic/versions/`

### Frontend: обязательно проверить

- [x] `frontend/src/pages/SyncPage.vue`
- [x] `frontend/src/pages/MRPResultPage.vue`
- [x] `frontend/src/services/api.ts`
- [x] `frontend/src/types/mrp.ts`
- [x] `frontend/src/composables/usePurchases.ts`
- [ ] новый `frontend/src/composables/useRework.ts`, если будет выбран отдельный composable
- [ ] компоненты grouped table, если потребуется выделение общего компонента

### Тесты: обязательно проверить

- [x] `tests/services/test_order_quantity_calculator.py`
- [x] тесты, покрывающие `planning_service`
- [x] тесты API для результатов MRP
- [ ] UI-проверки сценария сохранения групп и отображения rework

## Критерии готовности

- [x] В расчёте MRP существуют три потока: production, purchase, rework
- [x] `rework` сохраняется и выдаётся отдельно от закупки
- [x] Детали с методом `переработка` используют спецификации и учитывают дефицит комплектующих
- [x] Закупка больше не генерирует дробные количества для дискретных единиц
- [x] Production-поток не изменил свои результаты на контрольных сценариях
- [x] На странице синхронизации при повторном открытии видно сохранённые выбранные группы
- [x] Закупка отображается по товарным группам в UI
- [x] Переработка отображается по товарным группам в UI
- [x] Закупка выгружается в Excel по товарным группам
- [x] Переработка выгружается в Excel по товарным группам
- [x] Документация обновлена в `.docs/progress.md`

## Вопросы, которые ИИ должен перепроверять по ходу реализации

- [x] Есть ли в текущей модели номенклатуры явная ссылка на товарную группу, достаточная для группировки
- [x] Нужно ли резервирование компонентов активных 1С-заказов применять к `rework` полностью или частично
- [x] Нужна ли для `rework` собственная дата запуска, или достаточно `need_date` и `order_date`
- [x] Нужно ли поддерживать отдельные warning-коды для `rework`, или лучше переиспользовать production-коды
- [x] Не используется ли где-то на фронтенде предположение, что существует только два потока результатов

## Рекомендуемый порядок исполнения ИИ

- [x] Сначала выполнить безопасный рефакторинг классификации и общих helper-функций
- [x] Затем исправить закупку и покрыть это тестами
- [x] Затем добавить отдельную сущность и расчёт `rework`
- [x] Затем добавить grouped backend для закупки и переработки
- [x] Затем добавить экспорт Excel
- [x] Затем исправить и доработать frontend
- [x] В конце выполнить регрессию production и обновить документацию
