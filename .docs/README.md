# PRODPLAN — краткая документация (для человека + ИИ)

Цель этой папки — **давать минимум актуального контекста**, чтобы ИИ мог быстро вносить изменения в проект.

Важно:

- `.docs/prodplan-deploy.md` — **не часть базы знаний**, это шпаргалка по деплою (не редактировать).

## Быстрый старт (локально)

### Windows

```cmd
start.bat
```

### Linux/macOS

```bash
./start.sh
```

Адреса по умолчанию:

- Frontend: http://localhost:9000
- Backend Swagger: http://localhost:8000/docs

Если менялся код — пересборка:

```cmd
rebuild.bat
```

## Куда смотреть

- Как вести задачи с ИИ: `ai.md`
- Архитектура и зоны ответственности: `architecture.md`
- API: `api.md`
- База данных (коротко): `db_schema.md`
- Интеграция с 1С/OData (коротко): `odata.md`
- Типовые команды и диагностика: `troubleshooting.md`
- Текущее состояние/решения/известные проблемы: `progress.md`

## Принципиальные правила

1) **Один запрос — одно изменение.**
2) Любые изменения модели БД → миграция Alembic.
3) Контракты API не ломаем без явного решения.
