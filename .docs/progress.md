### Fix: MRP — унификация логики «Покраска» с модулем «Распределение этапов» (Сентябрь 2025)
- **Симптом**: В «Рекомендуемых заказах на производство» (страница результата прогона MRP) часть номенклатур (например, CP-000316-G) ошибочно попадала на малярный участок, тогда как в модуле «Распределение этапов» эти же позиции относились к другому этапу.
- **Диагноз**:
  - В функции планировщика [`run_planning_run()`](backend/app/services/planning_service.py:1173) внутренняя `analyze_parent_item` сначала корректно использует общий алгоритм [`determine_parent_stage_and_norm()`](backend/app/services/stage_logic.py:7), который определяет этап родителя ТОЛЬКО по единообразию `stage_id` у его непосредственных детей (или фиксирует неоднозначность).
  - Затем в MRP применялись дополнительные эвристики «покраски», которые перезаписывали результат:
    1) `FORCE_PAINTING_FROM_OPERATIONS` — если большинство операций спеки относилось к этапу «покраска», этап переписывался даже при однозначном этапе от детей ([`planning_service.py:1458`](backend/app/services/planning_service.py:1458)).
    2) `FORCE_PAINTING_BY_NAME` — по признакам в наименовании/артикуле/коде (цвет, суффиксы SP/GSP и т.п.) этап переписывался на «покраску» ([`planning_service.py:1489`](backend/app/services/planning_service.py:1489)).
  - Модуль «Распределение этапов» опирается только на единый `stage_id` прямых детей и не использует эвристики «по операциям»/«по имени»: [`calculate_resource_distribution()`](backend/app/services/resource_calculator.py:82). Поэтому результаты расходились.
- **Изменения (backend)**:
  - Ограничена эвристика `FORCE_PAINTING_FROM_OPERATIONS`: теперь она применяется ТОЛЬКО если базовый алгоритм вернул `stage_id = None` (нет детей/нет stage у детей/смешанные дети), что согласуется с [`determine_parent_stage_and_norm()`](backend/app/services/stage_logic.py:92). См. правку в [`planning_service.py:1458`](backend/app/services/planning_service.py:1458).
  - Отключена `FORCE_PAINTING_BY_NAME` по умолчанию и повешена на флаг конфигурации снапшота: `production.toggles.force_painting_by_name=false` (по умолчанию). Применяется только когда базовый алгоритм вернул `stage_id = None`. См. правку в [`planning_service.py:1489`](backend/app/services/planning_service.py:1489).
- **Эффект**:
  - Алгоритм MRP теперь согласован с модулем «Распределение этапов». Этап родителя определяется по единообразию `stage_id` его прямых детей (или остаётся неопределённым с пометкой причины), без агрессивного «переназначения на покраску» при наличии корректных данных в составе.
  - Ожидается исчезновение расхождений по позициям вроде CP-000316-G. Возможные остаточные расхождения — только при проблемах в данных (`NO_CHILD_STAGE` / `MIXED_CHILD_STAGES`) или при «Закупка» в карточке номенклатуры (тогда планируется закупка, а не производство).

### Fix: Остатки = 0 на странице «Распределение этапов» (Сентябрь 2025)
- **Проблема**: После успешной синхронизации остатков на странице «Распределение этапов» все значения "Остаток" показывались как 0.
- **Причина**: В распределение попадали только компоненты с методом пополнения «Производство», закупаемые позиции (даже имеющие `stage_id` в составе) отфильтровывались и не отображались с их фактическими остатками.
- **Изменения (backend)**:
  - В сервисе [`resource_calculator.py`](backend/app/services/resource_calculator.py) снят фильтр по методу пополнения при формировании аккумулятора компонент по этапам: теперь в расчёт и выдачу включаются все компоненты, если для них распознан `stage_id` (в т.ч. «Покупка»).
  - Поле `stock_qty` по-прежнему берётся из `Items.stock_qty`, которое обновляется синхронизацией OData.
- **Эффект**: Теперь в выдаче появятся и закупаемые компоненты с их фактическими остатками, если у строк состава задан `stage_id`.

### Методика: Распределение этапов по дереву спецификации и расчет норматива (Сентябрь 2025)
- **Единица распределения**: родительский узел (деталь/изделие) на уровне, где у него есть дети.
- **Этап вхождения родителя**: определяется ТОЛЬКО по прямым детям этого уровня. Если у всех детей указан ОДИН и тот же `stage_id` = S, то этап родителя = S. Никакого fallback к «внукам».
- **Листья** (материалы первой переработки): в распределение не попадают; они лишь определяют этап для родителя первого уровня ветви.
- **Норматив (norm_hours) узла**: сумма всех норм времени `SpecOperation.time_norm` спецификации РОДИТЕЛЯ на этом уровне.
- **Неоднозначности**: Узел помечается как `ambiguous: MIXED_CHILD_STAGES` (разные `stage_id` у детей) или `ambiguous: NO_CHILD_STAGE` (пустые `stage_id` у детей).
- **Кратность**: Каждое вхождение родительской детали в дереве изделия учитывается отдельно.
- **Статус**: Методика утверждена, рефакторинг backend в [`resource_calculator.py`](backend/app/services/resource_calculator.py) готовится к внедрению.

### UI/Frontend: Общие улучшения (Сентябрь 2025)
- **Исправление прокрутки и фиксация Layout**: Устранена проблема двойного скролла в `MainLayout.vue` путем стандартизации `QLayout` и удаления конфликтующих CSS. Шапка и боковая панель теперь корректно зафиксированы.
- **Нумерация строк в планах**: В таблицы дневного ([`PlanPage.vue`](frontend/src/pages/PlanPage.vue)) и квартального ([`PlanQuarterlyPage.vue`](frontend/src/pages/PlanQuarterlyPage.vue)) планов добавлена колонка «#» со сквозной нумерацией, закрепленная слева.

### Feature: Страница "Спецификация" (Дерево BOM) (Сентябрь 2025)
- **Цель**: Реализовать иерархическое представление состава изделия (BOM) для быстрого анализа.
- **API**: `GET /api/v1/specification/tree` (lazy-load) и `GET /api/v1/specification/full` (полное дерево).
- **Реализация**:
  - **Backend**: Разработаны сервисы, узлы дерева включают расчетные поля (`treeQty`, `treeTimeNh`) и предупреждения (`NO_STAGE`, `NO_TIME_NORM`, `CYCLE_DETECTED`).
  - **Frontend**: Создана страница `SpecificationPage.vue` с `QTable` в режиме дерева для просмотра полного состава.

### Feature: Синхронизация данных из 1С (Сентябрь 2025)
- **Синхронизация Единиц Измерения (ЕИ)**: Реализован сервис `units_sync.py` и эндпоинт `POST /api/v1/sync/units-odata`. Кнопка "Синхронизация номенклатуры + ЕИ" на странице `SyncPage.vue` теперь запускает обе синхронизации последовательно.
- **Синхронизация Наименований Операций**: Создан сервис `operations_sync.py` и эндпоинт `POST /api/v1/sync/operations-odata` для извлечения названий операций.

### Feature: Единый редактор квартального плана (Сентябрь 2025)
- **Цель**: Создать единую таблицу для редактирования квартального плана по неделям с возможностью ручного распределения по дням.
- **Ключевые правила**:
  - **Источник истины**: Таблица `production_plan_entries`.
  - **UI/UX**:
    - **Квартальная таблица** ([`PlanQuarterlyPage.vue`](frontend/src/pages/PlanQuarterlyPage.vue)): Редактируемая. Колонки недель с разворотом на 7 дней. Прошедшие периоды скрыты. Остаток недели (пятница) вычисляется автоматически. Включено автоповышение недельного плана, если сумма дней превышает его. Горизонт расширен до конца квартала + 4 недели. Колонки "Изделие" и "#" зафиксированы слева.
    - **Дневная таблица** ([`PlanPage.vue`](frontend/src/pages/PlanPage.vue)): Только для чтения (`read-only`). Отображает только будущие ненулевые значения из квартального плана.
- **API**: Используются существующие эндпоинты `query_plan_matrix_paginated()` и `bulk_upsert_plan_entries()`.

### Спецификация и реализация: Модуль планирования MRP (Сентябрь 2025)
- **Описание**: Двухконтурный MRP-модуль (дневной на 90 дней и недельный за его пределами) с backward-планированием, проверкой мощностей и приоритизацией.
- **Инфраструктура**:
  - **Миграции Alembic**: Создана структура для таблиц результатов планирования (`planning_run`, `planned_order` и др.). См. [`20250925_01_add_mrp_planning_tables.py`](backend/alembic/versions/20250925_01_add_mrp_planning_tables.py).
  - **ORM-модели**: Добавлены соответствующие модели в [`models.py`](backend/app/models.py).
- **Ключевые функции реализации**:
  - **Предпросчёт**: Реализованы эндпоинты для быстрого расчета валовой (`/calc_gross`) и чистой (`/calc_preview`) потребности без записи в БД.
  - **Полный прогон (`run_planning_run`)**:
    - **Lot sizing**: Применяются политики минимальной партии, кратности и округления для закупок и производства.
    - **Определение стадии**: Используется унифицированная логика из [`stage_logic.py`](backend/app/services/stage_logic.py), включая fallback на мажоритарный выбор по операциям спецификации.
    - **Backward scheduling**: Часы стадий распределяются назад от даты потребности с учетом доступной мощности участков (5/2, `daily_work_hours` × `capacity`).
    - **Динамическое распределение**: При наличии нескольких участков для этапа, система динамически выбирает наименее загруженный на конкретный день.
    - **Индекс приоритета**: Заказы и закупки получают `priority_index`, рассчитанный по формуле (критичность, важность, длительность цикла).
    - **Запись результатов**: Все результаты (заказы, стадии, закупки, загрузка, пеггинг) сохраняются в соответствующие таблицы.
- **API и Управление**:
  - **Результаты прогона**: Выдачи `/results/*` (`production`, `purchases`, `capacity`, `pegging`) поддерживают фильтрацию, пагинацию и сортировку.
  - **Управление конфигурацией**: Добавлены эндпоинты для версионирования конфигураций планирования (`/configs`, `/configs/active`).
  - **Политика хранения**: Реализован механизм очистки старых прогонов (`/cleanup`) и флаг `pinned` для защиты от удаления.
- **Устранение ошибок и улучшения**:
  - **NO_AREA_FOR_STAGE**: Добавлены инструкции по добавлению связей "этап-участок". Прогоны RUN 5 и 6 подтвердили, что после добавления связей и реализации динамического распределения система корректно обрабатывает ранее проблемные этапы.
  - **Нулевые нормо-часы**: Реализован механизм, гарантирующий создание `PlannedOrderStage` с `hours=0`, чтобы заказы без норм времени корректно группировались по участкам в UI.

### UI: Страница результатов MRP (MRPResultPage) (Сентябрь-Октябрь 2025)
- **Структура**: Результаты разделены на вкладки «Заказы на производство» и «Заказы на закупку».
- **Читабельность**:
  - Убраны технические ID из таблиц. Backend возвращает денормализованные `item_name`, `item_article` и человекочитаемую ЕИ (например, "шт" вместо GUID).
  - Количество форматируется с 3 знаками после запятой.
- **Индикаторы**: В заголовках групп участков отображаются: кол-во заказов, суммарный норматив, индикаторы срочности (`min_days_to_need`) и перегруза мощностей.
- **Режим «Задание на день»**: Добавлен календарь для выбора конкретного дня, который формирует повестку по участкам на эту дату, показывая дневные срезы стадий и загрузку.
- **Предупреждения**: Блок с предупреждениями теперь свернут по умолчанию для экономии места.
- **Экспорт**: Для обеих вкладок реализован экспорт в CSV и XLSX.
- **Исправления**:
  - Устранены ошибки `307 Redirect` и `500 Internal Server Error` при загрузке справочников (`items`, `resources`, `stages`) за счет унификации `axios`, исправления Pydantic-схем и настройки Nginx.
  - Исправлена ошибка загрузки списка прогонов (неправильный URL).

### UI: Страница синхронизации (SyncPage) (Сентябрь 2025)
- **Улучшение**: Синхронизация номенклатуры и единиц измерения объединена в одну кнопку «Синхронизация номенклатуры + ЕИ» для обеспечения консистентности данных. Процесс выполняется последовательно, с отображением общего прогресса.

### Feature: Переход на распределение по видам производства (Октябрь 2025)
- **Цель**: Замена привязки деталей к этапам производства на привязку к видам производства (Catalog_ВидыПроизводства из 1С) для более точного распределения по участкам.
- **Изменения (backend)**:
  - **Миграции**: Добавлены таблицы `production_kinds`, `resource_production_kinds`, поле `production_kind_id` в таблицу `specifications`.
  - **ORM-модели**: Созданы модели `ProductionKind`, `ResourceProductionKind`, обновлена модель `Specification` с добавлением связи к виду производства.
  - **Pydantic-схемы**: Добавлены схемы для новых моделей.
  - **Синхронизация**: Создан сервис `production_kind_sync.py` и эндпоинт `/v1/sync/production-kinds-odata` для синхронизации видов производства из 1С.
  - **Обновление логики**: Модифицирован сервис `specification_sync.py` для синхронизации поля `ВидПроизводства_Key` у спецификаций.
  - **Распределение**: Обновлены `resource_calculator.py` и `planning_service.py` для использования видов производства вместо этапов при распределении заказов по участкам.
- **Изменения (frontend)**:
  - **UI**: Добавлена кнопка «Синхронизация видов производства» на странице `SyncPage.vue`.
- **Эффект**: Более точное распределение заказов по участкам на основе видов производства из 1С, улучшенная гибкость системы планирования и распределения.

### Fix/Feature: Глобальная защита от дублей видов производства при привязке к участкам (Октябрь 2025)
- Требование: один вид производства может быть назначен только одному участку. В UI вид производства, уже назначенный на любом участке, исчезает из селектов добавления на всех остальных участках.
- Frontend: глобальная фильтрация доступных видов реализована в [availableKinds()](frontend/src/pages/ResourcesPage.vue:368). Теперь список исключает все production_kind_id, встречающиеся в любых связях `resource_production_kinds`.
- Backend: добавлена глобальная валидация в [add_production_kind_to_resource()](backend/app/routers/resources.py:202). Если `production_kind_id` уже назначен другому участку — HTTP 400 "Production kind already assigned to another resource". Сохранена идемпотентная проверка для текущего участка.
- DB (Alembic): добавлен уникальный констрейнт на `production_kind_id` в таблице `resource_production_kinds` с пред‑проверкой дублей в миграции [20251002_05_enforce_unique_pk_in_resource_production_kinds.py](backend/alembic/versions/20251002_05_enforce_unique_pk_in_resource_production_kinds.py). Это гарантирует глобальную уникальность на уровне БД.
- Документация: уточнено требование глобальной уникальности и UI‑поведение в [.docs/production_kind_migration_plan.md](.docs/production_kind_migration_plan.md); в [.docs/db_schema.md](.docs/db_schema.md) добавлен `UNIQUE(production_kind_id)` для `resource_production_kinds`.

Примечание по деплою:
- Прогнать миграции БД (после разрешения возможных дублей): `alembic upgrade head`
- Перезапустить backend‑сервис, чтобы подхватить изменения кода и схем.

### Fix: Прод — отсутствие столбца specifications.production_kind_id (Октябрь 2025)
- Симптом: при синхронизации спецификаций на проде возникала ошибка:
  psycopg2.errors.UndefinedColumn: column specifications.production_kind_id does not exist
- Контекст: код ожидает поле в модели [Specification.production_kind_id](backend/app/models.py:82), добавляемое миграцией [add_production_kinds_tables.upgrade()](backend/alembic/versions/20251002_04_add_production_kinds_tables.py:19), и заполняется при синхронизации в [sync_specifications_from_odata()](backend/app/services/specification_sync.py:31).
- Диагноз:
  - Alembic не знал текущую версию (alembic current ничего не показывал изначально).
  - Ранние таблицы уже существовали (частичный ручной/вне‑Alembic DDL ранее), из‑за чего upgrade пытался пересоздать существующие объекты и падал.
  - Таблицы production_kinds/resource_production_kinds были на проде, но колонка specifications.production_kind_id отсутствовала.
- Решение на проде (идемпотентно):
  1) Быстрая диагностика:
     ```
     docker compose exec -T db psql -U prodplan -d prodplan -Atc "SELECT to_regclass('public.production_kinds');"
     docker compose exec -T db psql -U prodplan -d prodplan -Atc "SELECT to_regclass('public.resource_production_kinds');"
     docker compose exec -T db psql -U prodplan -d prodplan -Atc "SELECT column_name FROM information_schema.columns WHERE table_schema='public' AND table_name='specifications' AND column_name='production_kind_id';"
     ```
  2) Добавили недостающие объекты для specifications:
     ```
     docker compose exec -T db psql -U prodplan -d prodplan -c "ALTER TABLE specifications ADD COLUMN IF NOT EXISTS production_kind_id INTEGER;"
     docker compose exec -T db psql -U prodplan -d prodplan -c "ALTER TABLE specifications ADD CONSTRAINT fk_specifications_production_kind FOREIGN KEY (production_kind_id) REFERENCES production_kinds(id);"
     docker compose exec -T db psql -U prodplan -d prodplan -c "CREATE INDEX IF NOT EXISTS ix_specifications_production_kind_id ON specifications(production_kind_id);"
     ```
  3) Проставили версию Alembic на состояние этой миграции:
     ```
     docker compose exec -T backend alembic stamp 20251002_04
     ```
  4) Контроль и перезапуск:
     ```
     docker compose exec -T backend alembic current
     docker compose restart backend
     ```
- Результат: alembic current показывает 20251002_04/HEAD, backend перезапущен, синхронизация спецификаций проходит без ошибок.
- Рекомендации по деплою:
  - После git pull обязательно выполнять alembic upgrade head, чтобы исключить дрейф схемы.
  - При несоответствии схемы и истории — выравнивать через alembic stamp на фактическую версию, затем применять недостающие миграции.
  - Опционально применить миграцию глобальной уникальности (может потребовать устранить дубли):
    ```
    docker compose exec -T backend alembic upgrade 20251002_05
    docker compose exec -T db psql -U prodplan -d prodplan -c "SELECT production_kind_id, COUNT(*) FROM resource_production_kinds GROUP BY production_kind_id HAVING COUNT(*) > 1;"
    ```

### Feature: Диагностика NO_AREA_FOR_PRODUCTION_KIND (Октябрь 2025)
- Требование: при отсутствии привязки вида производства к участку показывать окно с деталями (номенклатура, спецификация, проблемный вид).
- Backend:
  - Расширены предупреждения для случаев отсутствия привязки вида производства:
    - NO_AREA_FOR_PRODUCTION_KIND — теперь содержит: production_kind_id, production_kind_name, item_id, item_code, item_name, item_article, spec_id, spec_ref1c. Изменения внутри [run_planning_run()](backend/app/services/planning_service.py:1330), блок формирования предупреждения при пустых кандидатов для вида (см. добавку на участке выбора area по виду производства).
    - NO_AREA_FOR_PRODUCTION_KIND_ZERO_NORM — аналогично расширено: добавлены item/spec/pk поля для нулевых норм. Изменения в [run_planning_run()](backend/app/services/planning_service.py:1330) в ветке zero‑norm.
- Frontend:
  - На странице результатов MRP добавлена кнопка и диалог для проблем привязки:
    - Кнопка «Проблемы привязки видов» рядом с блоком предупреждений (см. [MRPResultPage.vue](frontend/src/pages/MRPResultPage.vue:78)).
    - Диалог с таблицей (Номенклатура, Артикул, Спецификация, Вид производства) — берёт данные из summary.warnings, фильтруя по code = 'NO_AREA_FOR_PRODUCTION_KIND' | 'NO_AREA_FOR_PRODUCTION_KIND_ZERO_NORM' (см. вычисления и колонки в [MRPResultPage.vue](frontend/src/pages/MRPResultPage.vue:584) и разметку диалога [MRPResultPage.vue](frontend/src/pages/MRPResultPage.vue:482)).
- Эффект:
  - При возникновении проблем по видам производства пользователь видит агрегированное окно с конкретными позициями и видами, что ускоряет назначение связей в UI «Производственные ресурсы».
- Примечания к деплою:
  - Миграций не требуется.
  - Для появления расширенных данных в окне необходимо запускать новые прогоны MRP после обновления backend — старые прогоны могли не содержать детализированных полей в warnings.
