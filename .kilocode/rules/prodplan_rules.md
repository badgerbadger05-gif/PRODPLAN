# prodplan_rules

- Живые доки — `.docs/`, индекс — `.docs/README.md`. Исполненное/одноразовое — в `.docs/archive/`.
- Изменил схему БД → миграция Alembic + обнови `.docs/db_schema.md`.
- Прогресс сессии — коротко в `.docs/progress.md` (файл держим ≤200 строк, дневники — в архив).
- Известные мины/долги — `.docs/tech-debt.md`; перед работой с остатками/резервами читай `.docs/item_ledger.md`.
- Контракты API не ломаем без явного решения.
