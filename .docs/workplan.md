# План работ

Постановка: **2026-05-25**. Актуальность задачи определяется по дате рядом с ней.

Формат: `[ ]` — не начато / `[~]` — в процессе / `[x]` — завершено.

---

## 1. Производственный контроль — выгрузка в 1С

> Все write-сервисы **уже реализованы** в коде (выяснено 2026-05-25 при ревью).
> Был баг: фронт вызывал несуществующие эндпоинты и слал неправильные поля.
> Обязательное правило: дочерние документы 1С создаются на основании родителя через `ДокументОснование` + `ДокументОснование_Type`; `Document_ЗаказНаПроизводство` и `Document_ЗаказПоставщику` из MRP являются первичными и создаются без основания.

- [x] **2026-05-25** Уточнить обязательные реквизиты 1С для `Document_ЗаказНаПроизводство` — готово, `one_c_production_order_export.py`
- [x] **2026-05-25** Реализовать write-сервис `Document_ЗаказНаПроизводство` — готово
- [x] **2026-05-25** Уточнить реквизиты `Document_ПеремещениеЗапасов` — готово, `one_c_stock_transfer_export.py`; создаётся на основании `Document_ЗаказНаПроизводство`
- [x] **2026-05-25** Реализовать write-сервис `Document_ПеремещениеЗапасов` — готово
- [x] **2026-05-25** Уточнить реквизиты `Document_СборкаЗапасов` для кнопки «Произвести» — готово, `one_c_manufacture_export.py`; создаётся на основании `Document_ЗаказНаПроизводство`
- [x] **2026-05-25** Реализовать write-сервис `Document_СборкаЗапасов` (акт выпуска) — готово
- [x] **2026-06-01** `Document_СдельныйНаряд` — write-сервис реализован: `one_c_piecework_export.py`, endpoint `POST /v1/production-control/manufactures/export-piecework-to-1c`; создаётся на основании `Document_СборкаЗапасов`, используется в flow «Произвести»
- [x] **2026-05-27** Синхронизация сотрудников `Catalog_Сотрудники` — готово: таблица `employees`, сервис `employee_sync.py`, endpoint `POST /api/v1/sync/employees-odata`, кнопка «Сотрудники» в интерфейсе синхронизации
- [x] **2026-05-26** Уточнить основание для `Document_ЗаказПоставщику`, создаваемого из MRP-потребности — первичный документ без 1С-основания

---

## 2. Производственный контроль — отслеживание статусов

- [x] **2026-05-25** Синхронизация проведённых перемещений `К перемещению → Собран` — готово, `one_c_posted_transfer_sync.py` + `POST /sync-posted-transfers`
- [x] **2026-06-01** Кнопка «Произвести» в журнале — готово: диалог quantity/executor + цепочка `/produce` → `/manufactures/export-to-1c` → `/manufactures/export-piecework-to-1c`
- [x] **2026-05-25** При частичном выпуске — обратное перемещение компонентов — готово, `return_leftover_components()` + `POST /orders/{id}/return-leftovers`
- [x] **2026-05-25** Исправлены API-рассогласования фронта: `start-in-1c` → `export-to-1c`, `sync-from-1c` → `sync-posted-transfers`, `produce-to-1c` → 2-шаговый flow, `product_ids` → `order_ids`, добавлен PATCH `/orders/{id}/quantity` (147 tests pass, lint clean, build ok)

---

## 3. Производственный контроль — остатки по складам

- [x] **2026-05-25** Добавить в локальную БД разрез остатков по складам — `item_warehouse_stock` уже существовал; `odata_stock_sync.py` синхронизирует per-(item, warehouse) разрез при синхронизации остатков
- [x] **2026-05-25** При выдаче материалов автоматически выбирать склад-источник, задавать вопрос только при неоднозначности — реализовано: `_auto_select_source_warehouse()`, при тай — диалог с radio-кнопками

---

## 4. Заказы поставщику — факт поступления

- [x] **2026-05-25** Выяснить документ 1С — `received_qty` хранится непосредственно в табличной части `Запасы` документа `ЗаказПоставщику` (поле `КоличествоПоступило`); отдельный документ поступления не нужен
- [x] **2026-05-25** Строки без `delivery_date` — учитываются (delivery_date=None в БД), уже реализовано в `supplier_order_sync.py`
- [x] **2026-05-25** Синхронизация `received_qty` — реализована в `supplier_order_sync.py` (поля `КоличествоПоступило`/`Получено`/`Поступило`)
- [x] **2026-05-25** Индикатор покрытия активными заказами поставщику — добавлен: `supplier_covered_qty` = `requested_qty - qty` в API `get_run_purchases`, колонка «Покрыто» в таблице MRP с % и цветом

---

## 5. Период план — мелкие доработки

- [x] **2026-05-25** Автогенерация поля `comment` при создании плана — реализована в `create_period_plan()`: «МАЙ 2026», «МАЙ–ИЮНЬ 2026» по `period_from`/`period_to`
- [x] **2026-06-01** Deep-link из строки журнала исполнения — production-order ведёт в журнал заказов, planned-order/purchase/rework ведут в соответствующую вкладку MRP-результата с подсветкой строки
- [x] **2026-05-25** Отображение для пустого списка групп — добавлен пояснительный текст в `SyncPage.tsx`: разные сообщения для случаев «список не загружен, но есть сохранённый выбор» и «список пуст»

---

## 6. Инфраструктура и технический долг

- [x] **2026-05-25** Удалить compatibility facade `backend/app/services/production_control.py` — удалён; внешних импортов не было; 148 тестов прошли
- [x] **2026-05-25** CI-документация и матрица команд добавлена в `.docs/README.md`: backend pytest, frontend lint, frontend build, Docker smoke; Playwright — TODO
- [ ] **2026-05-25** Formatter policy (auto-format enforcement поверх lint/build) — вопрос командный, не блокирует фичи; оставлен открытым

---

## 7. Журналы — читаемость и закупки

> Постановка 2026-06-11. Планы: `.docs/execution_journal_ux_plan.md`, `.docs/purchase_journal_plan.md`.

- [ ] **2026-06-11** Журнал исполнения, этап 1: колонка «Статус», тултипы терминов, скрытие строк «покрыто складом» по умолчанию
- [ ] **2026-06-11** Журнал исполнения, этап 2: перекомпоновка числовых колонок + колонка «Срок» (`need_date`/`status` в API журнала)
- [ ] **2026-06-11** Журнал исполнения, этап 3: сводка-полоса с кликабельными счётчиками; редизайн раскрытия work items
- [ ] **2026-06-11** Журнал закупок, бэк: `purchase_control_journal.list_journal` + роутер `/v1/purchase-control/*` + тесты
- [ ] **2026-06-11** Журнал закупок, фронт: страница `/purchase-control` по паттерну production-control (doctype, фильтры, detail pane, меню)
- [ ] **2026-06-11** Журнал закупок, перелинковка: deep-link из журнала исполнения и из закупок MRP-результата

---

## Приоритеты

| Приоритет | Группа | Обоснование |
|---|---|---|
| 🔴 Высокий | 1/2 — API-рассогласования | Исправлено 2026-05-25 |
| 🟢 Низкий | 1 — Document_СдельныйНаряд | Реализовано; остаются только проверки боевого сценария 1С |
| 🟡 Средний | 4 — факт поступления | Нетинг закупок неточен без `received_qty` |
| 🟡 Средний | 3 — остатки по складам | UX выдачи материалов |
| 🟢 Низкий | 5 — период план (мелкое) | Работает, мелкие удобства |
| 🟢 Низкий | 6 — инфраструктура | Технический долг, не блокирует фичи |
