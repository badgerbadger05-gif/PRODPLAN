name: prodplan-retired-9010

# RETIRED 2026-09-02.
# The former PRODPLAN stack on ports 8010/9010 must never be started again.
# This file intentionally contains no services so historical commands such as
# `docker compose -f docker-compose.test.yml up -d` cannot recreate it.
# The only live server contour is docker-compose.shadow.yml on 8020/9020.
services: {}
